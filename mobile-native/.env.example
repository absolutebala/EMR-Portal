# Copy to .env.local (git-ignored) and fill in real values.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Points at the deployed EMR Portal backend (AWS ECS/ALB — plain HTTP until a custom
# domain + ACM cert exist, see the AWS migration plan's Phase 6). Override to a LAN IP
# when testing against a locally-running `npm run dev` in the main emr-portal app —
# "localhost" from a physical device or simulator does not resolve to your Mac.
EXPO_PUBLIC_API_BASE_URL=http://EmrPor-Alb16-6ThX6OgVuHrJ-1597119572.ap-south-2.elb.amazonaws.com
